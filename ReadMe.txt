Copy everything to below path and restart gnome-shell (Alt+F2, r, Enter) to enable the extension.
Open extension manager to enable the extension. If you don't have extension manager, you can install it from your distribution's package manager or from https://extensions.gnome.org/local/.

open or create below path and copy the extension folder to it.

~/.local/share/gnome-shell/extensions